pluginManagement {
    repositories {
        google()
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter()
        maven { url 'https://www.jitpack.io' }
    }
}
rootProject.name = "GarbageSorting"
include ':app'
